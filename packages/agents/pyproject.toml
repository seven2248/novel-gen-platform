[project]
name = "novel-gen-agents"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "jsonschema>=4.23.0",
    "sqlmodel>=0.0.22",
]

[tool.pytest.ini_options]
testpaths = ["../../tests/agents"]
pythonpath = ["../../.."]
