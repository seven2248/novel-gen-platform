---
title: W16 小说生成系统开发记录 — 2026-04-16
date: "2026-04-16"
category: docs/
type: daily-log
week: W16
status: active
---

# W16 开发记录 — 2026-04-16（周四）

## Phase A Gap Closeout

执行 `2026-04-16-phase-a-gap-closeout.md` 计划，Phase A 收口。

### Task 1: Expose Project and Chapter State APIs ✅

**文件：**
- `services/api/app/main.py` — FastAPI app factory
- `services/api/app/routers/projects.py` — GET /projects/{project_id}, POST /projects/{project_id}
- `services/api/app/routers/chapters.py` — GET /projects/{project_id}/chapters/{chapter_id}, POST /projects/{project_id}/chapters, PATCH /projects/{project_id}/chapters/{chapter_id}/state（version conflict → 409）
- `tests/api/test_chapter_state_routes.py` — 5 个测试全过

**验证命令：**
```bash
pytest tests/api/test_chapter_state_routes.py -v
# 5 passed
```

### Task 2: Expose Main Flow and Local Rewrite APIs ✅

**文件：**
- `services/api/app/routers/agent_runs.py` — POST /agent-runs/main-flow
- `services/api/app/routers/local_rewrites.py` — POST /local-rewrites
- `tests/api/conftest.py` — autouse mock（scope: tests/api/），确保 route 测试不走 live endpoint
- `tests/api/test_agent_run_routes.py` — 2 个测试全过

**验证命令：**
```bash
pytest tests/api/test_agent_run_routes.py -v
# 2 passed
```

### Task 3: Minimum Regression Harness ✅

**文件：**
- `pyproject.toml` — 新增 `pyyaml>=6.0.2`
- `evals/cases/minimum_set.yaml` — 3 个 case（straight chapter, hook-strength, local rewrite）
- `evals/run_minimum_regression.py` — `load_suite()` + `run_case()` + `run_suite()`
- `evals/scorers/story_constraints.py` — `score_story_constraints()` + `score_local_rewrite()`
- `tests/evals/test_minimum_regression.py` — 4 个测试全过

**验证命令：**
```bash
pytest tests/evals/test_minimum_regression.py -v
# 4 passed
```

### Task 5: Closeout Verification ✅

**全量测试命令：**
```bash
pytest tests/api/test_chapter_state_routes.py tests/api/test_agent_run_routes.py tests/evals/test_minimum_regression.py -v
# 11 passed

# 全量回归
pytest
# 39 passed（包含 7 个 integration tests + 21 个 agent tests + 8 个新增 API/eval tests）
```

**附带的修复：**
- `tests/conftest.py` 新增 autouse `mock_llm` fixture，为 `tests/test_integration_flow.py` 补上了一直缺失的 LLM mock（注释写了要用 mock，但 root conftest 没有 autouse 覆盖）

### Phase A 收口结论

| 验收标准 | 状态 |
|----------|------|
| API routes 返回可用 JSON（project/chapter state/main flow/local rewrite） | ✅ |
| Minimum regression harness 可本地运行 | ✅ |
| Route tests 通过 scoped mock 离线运行 | ✅ |
| 全部 39 个测试无回归 | ✅ |
| Workspace shell（Task 4）| 降为 optional，未执行 |

**Workspace shell（Task 4）**：按计划降为 optional，不占本周 Phase A 收口定义。
